#include <iostream> //this is part of the standard library, this is for printing on the screen

//the main fn is the entry point to our program.
//This fn has to return an int number, if all is ok it returns 0, otherwise, another number is returned and is understood as an error
int main() {
  // *std -> is for the standard library
  // *cout -> character output
  std::cout << "Hello World";
  return 0;
}