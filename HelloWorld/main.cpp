#include <iostream> //this is part of the standard librery, this is for printing in the screen

//the main fn is the entry point to our program.
//This fn has to return a int number, if all is ok return 0, otherwise another number is returned and is understood as an error
int main() {
  // *std -> is for the standard librery
  // *cout -> character output
  std::cout << "Hello World";
  return 0;
}